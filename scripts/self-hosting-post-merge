#!/bin/sh
set -eu

# Integration merges run in candidate worktrees. Never install candidate code,
# refresh global guidance, or move dev from a merge hook.
printf '%s\n' "Self-hosting: keep the pinned coordinator; refresh it explicitly after validation."
