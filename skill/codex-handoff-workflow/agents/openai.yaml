interface:
  display_name: "Codex Handoff Workflow"
  short_description: "Record coding sessions and integrate completed commits one-shot"
  default_prompt: "Use $codex-handoff-workflow for this code-changing task from session start through one-shot integration."

policy:
  allow_implicit_invocation: true
